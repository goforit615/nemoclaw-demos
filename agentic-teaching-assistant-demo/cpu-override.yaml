# CPU override — removes the NVIDIA GPU device reservation from milvus-standalone
# and switches to the CPU-only Milvus image.
# Include this file with -f when running on a host without an NVIDIA GPU.
services:
  milvus:
    image: milvusdb/milvus:v2.6.5
    deploy:
      resources:
        reservations:
          devices: []
